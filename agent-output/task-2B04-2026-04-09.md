# task-2B04 handoff

## Spec Alignment

spec.md §4.6 Agent Status 表示 / §10 「`--color-danger` の追加と具体カラー値」未確定事項に対応。`error` ステータスを `raw 色値直書き禁止` 方針のまま描画可能にするための tokens 基盤整備。後続 task-2C01 (`AgentStatusBadge`) が `--color-danger` を参照して `error` バリアントを実装する前提を満たす。

## Summary

`src/styles/tokens.css` に red パレット 3 階調 (`--color-red-300/500/700`) と semantic alias 3 種 (`--color-danger`, `--color-danger-strong`, `--color-danger-bg`) を追加した。チケット指定通りの配置・値・構造を採用し、既存トークン (sage / gray / amber / `--accent-*` / `--border-subtle` / spacing / typography / radius) には一切手を加えていない。

### 追加内容

raw パレット (`amber` セクションの直後に追加):

```css
/* Red (danger) — Phase 2 で導入 */
--color-red-300: #f08a8a;
--color-red-500: #d04848;
--color-red-700: #8e2828;
```

semantic aliases (`--accent-attention` の下、`--border-subtle` の上に追加):

```css
--color-danger: var(--color-red-500);
--color-danger-strong: var(--color-red-700);
--color-danger-bg: var(--color-red-700);
```

### 配置検証

- raw パレットは `--color-amber-700` (line 21) の直後 (line 23-26) に配置 → sage / gray / amber → red の順序を維持
- semantic alias は `--accent-attention` (line 36) の下、`--border-subtle` (line 40) の上 (line 37-39) に配置 → 既存 alias 順序を破壊していない
- 3 階調 + 3 alias の構造を厳守

## Files

- `/Users/kzt/Desktop/project-d/product/quietmem/src/styles/tokens.css` (修正)

## tests_run

### `pnpm build`

成功。出力:

```
> quietmem@0.1.0 build /Users/kzt/Desktop/project-d/product/quietmem
> tsc -b && vite build

vite v5.4.21 building for production...
transforming...
✓ 84 modules transformed.
rendering chunks...
computing gzip size...
dist/index.html                   0.39 kB │ gzip:  0.27 kB
dist/assets/index-DkZKSWnW.css   38.02 kB │ gzip:  5.54 kB
dist/assets/index--5DnnBVB.js   182.84 kB │ gzip: 57.28 kB
✓ built in 298ms
```

`tsc -b` (型チェック) と vite production build が pass。CSS variable 追加なので TS 型への影響なし。生成された CSS バンドル (`dist/assets/index-DkZKSWnW.css`) に `--color-danger` トークンが含まれる前提でバンドルが通っている。

### vite dev server / DevTools での `:root` 確認

スキップ。Tauri アプリ環境のため vite dev 単独での起動を行わず、build pass で代替検証とした (チケット本文「vite dev サーバ起動が困難ならスキップ可」に該当)。

## completed

- `src/styles/tokens.css` に `--color-red-300/500/700` の 3 階調を追加
- `src/styles/tokens.css` に `--color-danger`, `--color-danger-strong`, `--color-danger-bg` の semantic alias を追加
- 配置場所 (raw は amber 直後 / alias は `--accent-attention` の下 `--border-subtle` の上) をチケット指示通りに反映
- 既存トークン (sage / gray / amber / spacing / typography / radius / 既存 semantic alias) を一切変更していないことを確認
- `pnpm build` (`tsc -b && vite build`) が success で完了

## in_progress

なし

## blocked

なし

## known_gaps

- vite dev server の DevTools で実際に `:root` の computed style に `--color-danger: #d04848` が出ているかは未確認 (Tauri 環境のため build pass で代替)。後続の `AgentStatusBadge` (task-2C01) 実装時に `var(--color-danger)` が解決されることが実描画で再確認される

## next_step

1. task-2C01 (`AgentStatusBadge` コンポーネント実装) で本トークンを参照し、`error` バリアントの配色 (`background: var(--color-danger-bg); color: var(--fg-primary); border: 1px solid var(--color-danger-strong);` 等) を組む
2. 必要に応じて `--color-red-300` を alert banner の薄背景として活用 (Phase 2 では未使用、余地確保)
