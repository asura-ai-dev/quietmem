# task-2B03 handoff (2026-04-09)

## Spec Alignment

spec.md §6 / §12-7 / §13.4 — `agentStore.duplicateAgent(...)` 追加。Phase 2 受け入れ条件 7.4「複製が `agent_duplicate` 経由で実行される」をフロント store 層から成立させるための一手で、UI 統合 (Phase 2D) の前段。

## Summary

`src/store/agentStore.ts` に `duplicateAgent` action を追加した。`agentService.duplicate(input)` を呼び、戻り値の `agent.projectId` で `refreshAgents` を実行して一覧キャッシュを再取得する。`loading` / `error` state は既存と共有し、例外時は `error` を更新したうえで throw する (Phase 1 の `createAgent` / `updateAgent` と同パターン)。

実装方針:

- `AgentDuplicateInput` 型を import に追加
- `AgentState` インターフェースの `updateAgent` 直後に `duplicateAgent` シグネチャを追加
- store 実装の `updateAgent` 直後に action 本体を追加
- 楽観更新なし (`refreshAgents` で再取得)
- 戻り値の `agent.projectId` を使用 (input 側には存在しないため)
- 既存 `refreshAgents` / `createAgent` / `updateAgent` / Worktree 系には一切手を入れていない (回帰防止)
- Phase 1F task-1F06 で修正された `?? []` セレクタ問題は store の構造を維持しているため再発しない (cache 構造 / setter 形 / Promise return 型は既存と同形)

## Files changed

- `/Users/kzt/Desktop/project-d/product/quietmem/src/store/agentStore.ts`
  - import: `AgentDuplicateInput` 追加
  - `AgentState` インターフェース: `duplicateAgent: (input: AgentDuplicateInput) => Promise<Agent>;` 追加
  - store 実装: `duplicateAgent` action 本体追加 (`updateAgent` の直後)

## Files read (no edits)

- `/Users/kzt/Desktop/project-d/product/quietmem/agent-docs/spec.md`
- `/Users/kzt/Desktop/project-d/product/quietmem/tasks/phase-2B/task-2B03.md`
- `/Users/kzt/Desktop/project-d/product/quietmem/src/services/agentService.ts` (task-2B02 で追加済の `duplicate` メソッドの存在確認)
- `/Users/kzt/Desktop/project-d/product/quietmem/src/types/bindings.ts` (`AgentDuplicateInput` 定義の存在確認、line 93 で確認)

## Verification

### `pnpm tsc --noEmit`

```
EXIT=0
```

型エラーなし。

### `pnpm build`

```
> quietmem@0.1.0 build
> tsc -b && vite build

vite v5.4.21 building for production...
transforming...
✓ 84 modules transformed.
rendering chunks...
computing gzip size...
dist/index.html                   0.39 kB │ gzip:  0.27 kB
dist/assets/index-BDJ6IO62.css   37.83 kB │ gzip:  5.49 kB
dist/assets/index-b4TuUT_s.js   182.84 kB │ gzip: 57.28 kB
✓ built in 303ms
```

tsc と vite 両方 pass。

## Progress

### completed

- `AgentDuplicateInput` の import 追加
- `AgentState` インターフェースに `duplicateAgent` シグネチャ追加
- store 実装に `duplicateAgent` action 追加 (refresh を含む)
- `pnpm tsc --noEmit` pass
- `pnpm build` pass

### in_progress

- なし

### blocked

- なし

### tests_run

- `pnpm tsc --noEmit` → EXIT=0 (pass)
- `pnpm build` → tsc build + vite build 両方 pass
- ユニットテストは Phase 1 同方針で未追加 (チケットでも要求されていない)

### known_gaps

- ランタイム動作確認 (実際に複製が走り refresh が発火するか) は UI 統合フェーズ (Phase 2D) で確認される。本チケットは store 層単体の追加で完結。
- store ユニットテストはなし (Phase 1 から継続の方針)

### next_step

1. task-2B04 以降 (UI フォーム / 一覧の複製ボタン統合) で `useAgentStore.duplicateAgent` を呼び出す UI を実装する
2. UI 統合後の Smoke で「複製ボタン → store action → refreshAgents → 一覧再描画」までを通しで確認する
