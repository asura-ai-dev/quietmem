# task-2C06 handoff

## Spec Alignment

spec.md §4.7 (LeftSidebar 統合) で「LeftSidebar の Agent 行 click と Overview の編集ボタン click の双方で `selectedAgentId` を同じ store キーで管理する」と明記されており、§10 では `selectedAgentId` の置き場所 (uiStore / agentStore / ローカル) を実装フェーズで決定するとされている。本チケットは task-2B05 で uiStore に追加済みの `selectedAgentId` / `setSelectedAgentId` を OverviewTab から参照するよう差し替え、LeftSidebar (task-2C05) と Overview 編集フォームを同一の真実源で連動させる。

## frontend-design 確認結果

**視覚変更: ゼロ**

state のソースを React ローカル `useState` から zustand `useUiStore` に昇格するだけで、レンダリング出力・DOM・className・CSS module・子コンポーネント props シグネチャ・レンダリング条件はすべて不変。

- DOM ツリー: 変更なし (`<div className={styles.root}>` 以下の JSX は触らず)
- className: 変更なし (`OverviewTab.module.css` は touch しない)
- 子コンポーネント props: `AgentList.onSelect` / `AgentEditForm.onSaved` に渡す関数の実体は変わるが、シグネチャ `(id: string | null) => void` は同一
- 初期値: uiStore 側も `selectedAgentId: null` で初期化されており初回レンダリング状態は同一
- 既存の sage-tinted refined minimalism (Phase 1E/1F 確立済み) をそのまま維持

## 実装内容 (変更 4 箇所)

ファイル: `src/tabs/OverviewTab.tsx`

### 1. import 拡張 (line 1, 11)

```tsx
// Before
import { useEffect, useState, type ReactNode } from "react";
// ... 他 import
import type { Agent, Worktree } from "../types/bindings";

// After
import { useEffect, type ReactNode } from "react";
// ... 他 import
import { useUiStore } from "../store/uiStore";
import type { Agent, Worktree } from "../types/bindings";
```

- `useState` を react import から削除
- `useUiStore` を `../store/uiStore` から新規 import (import 順序は他 store に続いてアルファベット順を維持)

### 2. ローカル useState → uiStore selector (line 72-76)

```tsx
// Before
const [selectedAgentId, setSelectedAgentId] = useState<string | null>(null);

// After
// task-2C06: 選択中 agent は uiStore に昇格し、LeftSidebar Agents セクション
// (task-2C05) と OverviewTab の編集フォームで同一の真実源を共有する。
// 参照: spec.md §4.7 / §10, agent-docs/phase-2-ui-design.md §6
const selectedAgentId = useUiStore((state) => state.selectedAgentId);
const setSelectedAgentId = useUiStore((state) => state.setSelectedAgentId);
```

### 3. Project 切替時 reset useEffect の deps 拡張 (line 90-94)

```tsx
// Before
useEffect(() => {
  setSelectedAgentId(null);
}, [selectedProjectId]);

// After
// setSelectedAgentId は zustand の action なので識別子は安定しているが、念のため deps に含める
useEffect(() => {
  setSelectedAgentId(null);
}, [selectedProjectId, setSelectedAgentId]);
```

### 4. 選択中 agent 消失時 reset useEffect の deps 拡張 (line 97-104)

```tsx
// Before
useEffect(() => {
  if (
    selectedAgentId &&
    !agentsForProject.some((a) => a.id === selectedAgentId)
  ) {
    setSelectedAgentId(null);
  }
}, [selectedAgentId, agentsForProject]);

// After
useEffect(() => {
  if (
    selectedAgentId &&
    !agentsForProject.some((a) => a.id === selectedAgentId)
  ) {
    setSelectedAgentId(null);
  }
}, [selectedAgentId, agentsForProject, setSelectedAgentId]);
```

## 既存挙動の維持 (非変更)

- `AgentList` / `AgentCreateForm` / `AgentEditForm` の props インターフェースは変更なし
- `AgentList` の `onSelect` callback には引き続き `setSelectedAgentId` を渡す (uiStore 経由)
- `AgentEditForm` の `onSaved` は引き続き `() => setSelectedAgentId(null)` を渡す
- `Section` / `SectionProps` / placeholder UI / JSX 構造: 一切変更なし
- `agentsForProject` / `worktreesForProject` の zustand selector および `EMPTY_AGENTS` / `EMPTY_WORKTREES` 定数: Phase 1F の `Maximum update depth exceeded` 回帰防止のため変更なし
- `selectedAgent` 算出ロジック: 同一

## ファイル一覧

- 変更: `/Users/kzt/Desktop/project-d/product/quietmem/src/tabs/OverviewTab.tsx`
- 参照のみ (変更なし): `/Users/kzt/Desktop/project-d/product/quietmem/src/store/uiStore.ts` (task-2B05 で selectedAgentId / setSelectedAgentId 追加済)

## 検証結果

### pnpm tsc --noEmit

- exit 0 (エラー・警告なし、stdout 空)

### pnpm build

- exit 0
- `✓ 87 modules transformed.`
- `dist/index.html                   0.39 kB │ gzip:  0.27 kB`
- `dist/assets/index-DiYb8XJv.css   40.27 kB │ gzip:  5.89 kB`
- `dist/assets/index-4RiMrEQY.js   184.45 kB │ gzip: 57.87 kB`
- `✓ built in 296ms`
- CSS バンドルサイズは Phase 1F 時点 (40.xx kB) からほぼ変化なし → 視覚変更ゼロを裏付け

### completed

- OverviewTab.tsx の selectedAgentId を `useState` から `useUiStore` selector に置換
- `useState` の React import 削除
- `useUiStore` import 追加
- 2 つの useEffect の依存配列に `setSelectedAgentId` 追加
- `pnpm tsc --noEmit` 通過
- `pnpm build` 通過

### in_progress

- なし

### blocked

- なし

### tests_run

- `pnpm tsc --noEmit` → pass (exit 0, no output)
- `pnpm build` → pass (exit 0, 87 modules transformed, 296ms)
- 手動 Smoke (Project 切替時 reset / LeftSidebar ↔ Overview 連動) は task-2D03 で実施予定のため本チケットでは未実行 (チケット本文で明示)

### known_gaps

- LeftSidebar ↔ Overview の実連動動作確認は手動 Smoke が必要 (task-2D03)
- Project A → Agent X 編集ボタン → Project B 切替で editForm が閉じることの end-to-end 確認も task-2D03

### next_step

1. task-2C07 (Phase 2C の次チケット) に進む
2. Phase 2C 終盤で task-2D03 の手動 Smoke を実施し、LeftSidebar ↔ Overview の `selectedAgentId` 連動を確認

## Out of scope (非変更)

- LeftSidebar 側の差し替え (task-2C05 で完了済)
- 複製ボタン (task-2D02)
- AgentList の編集ボタンロジック変更
- CSS module / visual tokens
