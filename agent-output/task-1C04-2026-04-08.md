# task-1C04 Handoff — Tauri commands - worktree

## Spec Alignment

spec.md §2.2 バックエンド層 / §4.3 Tauri commands (CRUD) の
`worktree.create` / `worktree.list_by_project` / `worktree.update` に対応する。
§5.1 受け入れ条件「Worktree を作成できる。作成した Worktree が Project ごとに
一覧に表示される」のバックエンド公開口を Phase 1C で完成させる。

## Ticket

- ticket_path: `tasks/phase-1C/task-1C04.md`
- refs: `agent-docs/tauri-commands.md`
- depends_on: task-1C03 (agent commands, 完了済み)

## Summary

`src-tauri/src/commands/worktree.rs` に 3 つの Tauri command を実装した。
repo 層 (`db::repo::worktree`) は task-1B06 で完成しており、command は
`AppState::with_conn` 経由で薄く委譲する形に徹している。
1C03 (agent commands) と完全に同型のパターンで、相互比較が容易。

`invoke_handler` への登録は次チケット task-1C05 の責務なので、本ファイルでは
関数を `pub` で公開するのみ (Builder への配線は行っていない)。

## Files Changed

- `src-tauri/src/commands/worktree.rs`
  - 雛形 (コメントのみ 5 行) から以下を追加:
    - `use` 文 4 行 (`tauri::State`, `AppState`, `db`, `domain::worktree::*`, `error::AppResult`)
    - `#[tauri::command(rename_all = "camelCase")] pub fn worktree_create(state, input: WorktreeCreateInput) -> AppResult<Worktree>`
    - `#[tauri::command(rename_all = "camelCase")] pub fn worktree_list_by_project(state, project_id: String) -> AppResult<Vec<Worktree>>`
    - `#[tauri::command(rename_all = "camelCase")] pub fn worktree_update(state, input: WorktreeUpdateInput) -> AppResult<Worktree>`
  - 各 command は `state.with_conn(|conn| db::repo::worktree::xxx(conn, ...))` の 1 行委譲

変更なし (整合性維持):

- `src-tauri/src/commands/mod.rs` — 既に `pub mod worktree;` が宣言済み
- `src-tauri/src/domain/worktree.rs` — DTO は 1B06 で定義済み (`Worktree`, `WorktreeCreateInput`, `WorktreeUpdateInput`)
- `src-tauri/src/db/repo/worktree.rs` — `create` / `list_by_project` / `update` および単体テストは 1B06 で実装済み

## Implementation Notes

- 1C03 (agent commands) と同じパターン:
  - `#[tauri::command(rename_all = "camelCase")]` で `project_id` 引数が TS から `{ projectId }` で呼べるようにする
  - `state.with_conn(...)` でロック失敗時の `AppError::Internal` 変換を集約
  - doc comment で spec / tauri-commands.md の該当箇所を明記
- `worktree_list_by_project` の第 2 引数は `project_id: String` (1C03 の `agent_list_by_project` と同じシグネチャ)
- Rust 関数名と Tauri command 名は一致 (rename はせず、Rust 側 snake_case をそのまま公開)
- バリデーションや DB ロジックは repo 層に完全委譲しており、command 層は純粋なアダプタ

## 検証

### cargo check

```
cd src-tauri && cargo check
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.75s
```

warning ゼロ。

### cargo test

```
cd src-tauri && cargo test
running 39 tests
...
test result: ok. 39 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.07s
```

- 39 件全 pass (repo 層 worktree 関連 11 件を含む)
- 新規テストは追加していない (command 層は薄いアダプタで、repo 層に既に包括的テストがあるため。1C03 と同方針)

## completed

- `worktree_create` command 実装 (repo::worktree::create への委譲)
- `worktree_list_by_project` command 実装 (`project_id: String` 引数、`{ projectId }` camelCase 対応)
- `worktree_update` command 実装 (repo::worktree::update への委譲)
- `rename_all = "camelCase"` の付与 (3 command すべて)
- doc comment で spec / tauri-commands.md 参照を明記
- cargo check warning ゼロ確認
- cargo test 39 件全 pass 確認

## in_progress

なし

## blocked

なし

## tests_run

- `cd src-tauri && cargo check` → warning なしで成功
- `cd src-tauri && cargo test` → 39 passed / 0 failed

## known_gaps

- `invoke_handler` への登録は未実施 (task-1C05 の責務)
- command 層単体のテストは追加していない。repo 層に包括的テスト (11 件) があり、command は 1 行委譲のため。統合テストが必要な場合は task-1C05 以降の smoke で担保される想定。
- フロントエンド側の `worktreeService.ts` / TypeScript 型 (`Worktree`, `WorktreeCreateInput`, `WorktreeUpdateInput`) は別チケット (Phase 1D) で追加予定

## next_step

1. task-1C05: `lib.rs` の `invoke_handler` に 9 command (project × 3, agent × 3, worktree × 3) を登録する
2. Phase 1D: TS 側 `bindings.ts` / `worktreeService.ts` の追加 (fronend 統合)
3. Phase 1 統合 smoke: create → list → update の E2E を Workspace Shell から通す
