# task-1D05 handoff — デザイントークンと global スタイル

## Spec Alignment

spec.md §4.6 デザイントークン（セージグリーン / ダークグレー / アンバーを CSS 変数として一元化）および §5.2 QTM-002 受け入れ条件「3 色がデザイントークンとして定義され Shell 内で使われている」の基盤部分に対応。後続 UI チケットが参照する CSS 変数レイヤーのみを先出しで確定させる。

## Phase

Phase 1D / implement（単独チケット）

## Files Changed

- `src/styles/tokens.css` (新規): `agent-docs/ui-shell.md` §デザイントークン の CSS 変数定義を厳密に転記
- `src/styles/global.css` (新規): 最小リセット + body の bg/fg/font 既定 + button/input/select/textarea の基本スタイル（背景透過・色継承・`--border-subtle` のボーダー）
- `src/main.tsx` (更新): 既存の react / App import を保持したまま `import "./styles/tokens.css";` と `import "./styles/global.css";` を追加

## Completed

- tokens.css に ui-shell.md の全 CSS 変数を転記
  - sage 50/200/400/500/600/700
  - gray 50/200/400/600/800/900
  - amber 300/500/700
  - semantic aliases（`--bg-app`, `--bg-surface`, `--bg-surface-2`, `--fg-primary`, `--fg-muted`, `--accent-primary`, `--accent-primary-hover`, `--accent-attention`, `--border-subtle`）
  - spacing 1/2/3/4/6/8
  - typography（`--font-sans`, `--font-mono`, `--font-size-sm/base/lg/xl`）
  - radius sm/md/lg
- global.css に最小リセット
  - `*, *::before, *::after { box-sizing: border-box; }`
  - `html, body, #root { height: 100%; margin: 0; padding: 0; }`
  - `body` に `--bg-app` / `--fg-primary` / `--font-sans` / `--font-size-base` 適用
  - 見出し・段落・リストのマージンリセット
  - `button` / `input` / `select` / `textarea` は `background: transparent; color: inherit; font: inherit; border: 1px solid var(--border-subtle); border-radius: var(--radius-sm);`
  - focus 時に `outline: 2px solid var(--accent-primary)`
- main.tsx の既存 import を残したまま tokens.css → global.css の順で import を追加（順序重要: tokens が global の変数解決に使われる）

## in_progress

なし

## blocked

なし

## tests_run

- `pnpm tsc --noEmit` → EXIT=0（型エラーなし）
- `pnpm build` → EXIT=0
  - vite build 成功
  - `dist/assets/index-Bfi20ZO_.css 1.96 kB` が生成されており、新規 CSS が実際にバンドルに組み込まれたことを確認

## known_gaps

- 目視での配色確認（アプリ起動してダークグレー背景が描画されること）はこのチケットでは行っていない。Shell 実装チケット（後続 1D06+）で自然に確認される見込み
- `@media (prefers-color-scheme: light)` 対応は spec/ui-shell.md の方針通り Phase 1 では対象外

## next_step

- 後続（並列実行中の 1A03/1A04/1D03 や Shell 実装チケット）が `var(--bg-app)` などセマンティックエイリアスを参照できる状態。Shell 実装チケットで token が実際に使われているかを評価フェーズで確認する
- `src/main.tsx` は task-1A03 と衝突しうるため、Orchestrator 側で最終的に両者の import が両方残っていることを確認すること（現時点では react/App import のみを保持した上で styles 2 行を追加済み）

## 重要判断

- tokens.css の内容は ui-shell.md からの **厳密転記**（1 文字も変更していない）。後続チケットが変数名ズレで壊れないよう最優先した
- global.css は normalize.css 等の外部依存を入れず手書きで最小化。スコープ外の広範なベーススタイル（例: `table`, `pre`, `code` の細かい調整）は意図的に含めていない
- `padding: 0` は `html, body, #root` にも追加（ブラウザデフォルトのマージンだけでなくパディングも明示的にリセット）
