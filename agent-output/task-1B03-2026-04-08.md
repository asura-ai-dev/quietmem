# task-1B03 handoff: 初期スキーマ (001_init.sql)

## Spec Alignment

spec.md §4.1 / §4.1.1 / §5.1 の「SQLite 初期化とスキーマ」「テーブル定義 (最小カラム)」「DB 受け入れ条件」に対応する。Phase 1 で必要な 6 テーブル (projects / agents / worktrees / runs / raw_memory_entries / curated_memories) の DDL を `001_init.sql` に書き起こし、既存マイグレーション runner から適用可能にする。

## Phase

Phase 1B / task-1B03 完了。

## Completed

- `src-tauri/src/db/migrations/001_init.sql` に db-schema.md の 6 テーブル DDL を転写
  - `projects` (+ `idx_projects_slug`)
  - `agents` (FK project_id CASCADE, + `idx_agents_project`)
  - `worktrees` (FK project_id CASCADE, + `idx_worktrees_project`, `idx_worktrees_agent`)
  - `runs` 骨格 (+ `idx_runs_project`, `idx_runs_agent`)
  - `raw_memory_entries` 骨格 (+ `idx_raw_memory_project`, `idx_raw_memory_agent`)
  - `curated_memories` 骨格 (+ `idx_curated_project`, `idx_curated_agent`)
- db-schema.md に合わせて以下を厳守
  - カラム名 / 順序 / NOT NULL / DEFAULT 値 / PRIMARY KEY / UNIQUE をすべて転写
  - `agents.active_worktree_id` と `worktrees.agent_id` には FK を張らない (循環回避)
  - `runs` / `raw_memory_entries` / `curated_memories` には FK を張らない
  - `CREATE TABLE IF NOT EXISTS` ではなく素直な `CREATE TABLE` (冪等性は runner が担保)
  - ステートメントは `;` + 改行で区切り、`execute_batch` で一括適用可能
- 既存の migration runner テスト (task-1B02 由来) を実行し、すべて pass することを確認
  - `run_pending_applies_initial_migration`
  - `run_pending_is_idempotent`
  - `run_pending_applies_when_meta_table_preexists`
- `cargo check` / `cargo test --lib` がエラーなく完了
- **fix (2026-04-08)**: Phase 1 の 6 テーブル存在確認テスト `run_pending_creates_all_phase1_tables` を `migration.rs` の `#[cfg(test)] mod tests` 末尾に追加。`sqlite_master` を `type='table' AND name=?1` で問い合わせ、期待する 6 テーブル (projects / agents / worktrees / runs / raw_memory_entries / curated_memories) がすべて 1 件ずつ存在することを検証する

## In Progress

なし

## Blocked

なし

## Tests Run

- `cargo check` (src-tauri): ok (warnings なし / 0 errors)
- `cargo test --lib` (src-tauri): 9 passed / 0 failed (初回)
- **fix 後**: `cargo test --lib db::migration` (src-tauri): 4 passed / 0 failed
  - `db::migration::tests::run_pending_applies_initial_migration` ok
  - `db::migration::tests::run_pending_is_idempotent` ok
  - `db::migration::tests::run_pending_applies_when_meta_table_preexists` ok
  - `db::migration::tests::run_pending_creates_all_phase1_tables` ok (新規追加)
- `cargo build --lib` (src-tauri): ok (warnings ゼロ維持)

上記のマイグレーション runner テストは `001_init.sql` を実際に `execute_batch` で流しており、SQL 構文エラーがあれば fail するため、DDL の構文妥当性も間接的に検証されている。新規追加した `run_pending_creates_all_phase1_tables` により、6 テーブルの実体が `sqlite_master` に存在することを機械的に保証できるようになった。

## Known Gaps

- 「各カラムが期待通り生えているか」を直接 `PRAGMA table_info` 等で確認するテストは未追加。ただしスコープ外であり、後続 1B04 以降の repo 層テスト (projects / agents / worktrees の CRUD テスト) で実質的にカバーされる想定。
- ~~`runs` / `raw_memory_entries` / `curated_memories` のテーブル存在確認テスト未追加~~ → fix で `run_pending_creates_all_phase1_tables` を追加し解消済み。

## Next Step

1. task-1B04 以降で `db::repo::projects` / `agents` / `worktrees` の CRUD 実装 (DDL を前提に使える状態)
2. その際、`runs` / `raw_memory_entries` / `curated_memories` のテーブル存在確認テスト (`SELECT 1 FROM ... LIMIT 0`) も追加しておくとよい
3. Orchestrator は `/verify tasks/phase-1B/task-1B03.md` で done_when を機械的に確認する

## Files Changed

- `src-tauri/src/db/migrations/001_init.sql` (空ファイル → 6 テーブル + 10 インデックスの DDL)
- `src-tauri/src/db/migration.rs` (fix: `#[cfg(test)] mod tests` に `run_pending_creates_all_phase1_tables` を追加)

変更なし:

- `src-tauri/src/db/connection.rs`
- `src-tauri/src/db/mod.rs`
