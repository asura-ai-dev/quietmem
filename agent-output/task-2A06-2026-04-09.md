# task-2A06 handoff (2026-04-09)

## Spec Alignment

spec.md の 2.2 Backend 層 / 5.2 アプリケーション層の status enum / 6 Tauri commands 追加 / 13.4 API 評価観点 に対応する。Phase 2A で追加したバックエンド実装 (status ホワイトリスト検証 + `agent_duplicate` command) の品質ゲート (build / test / clippy) を確認した。

## Verification Commands & Results

cwd: `/Users/kzt/Desktop/project-d/product/quietmem/src-tauri`

| Step | Command                                     | Result                                                     |
| ---- | ------------------------------------------- | ---------------------------------------------------------- |
| 1    | `cargo build`                               | Finished `dev` profile, errors 0, warnings 0               |
| 2    | `cargo test --lib`                          | **58 passed; 0 failed; 0 ignored; 0 filtered out** (0.09s) |
| 3    | `cargo clippy --all-targets -- -D warnings` | Finished `dev` profile, warnings 0                         |

全ステップ pass。コード変更なし（3 行以下の軽微な clippy fix も不要だった）。

## Test Count Comparison

- Phase 1 完了時点: **39 tests**
- Phase 2A 完了時点 (今回): **58 tests**
- 差分: **+19 件** (仕様上の期待 +15 件 / status 検証 5 + duplicate 8 + DTO deserialize 2 を上回る)

### Phase 2A で追加された主なテスト (grep で確認)

domain::agent::tests (7 件):

- `agent_status_values_contains_exactly_four_values`
- `validate_agent_status_accepts_all_four_values`
- `validate_agent_status_rejects_unknown_value`
- `validate_agent_status_rejects_empty_string`
- `validate_agent_status_is_case_sensitive`
- `agent_duplicate_input_deserializes_from_camel_case`
- `agent_duplicate_input_name_is_optional`

db::repo::agent::tests の新規分 (約 12 件):

- status 検証系: `create_accepts_all_four_status_values`, `create_with_invalid_status_returns_invalid_input`, `update_accepts_needs_input_status`, `update_with_invalid_status_returns_invalid_input`
- duplicate 系: `duplicate_returns_new_agent_with_copied_fields`, `duplicate_starts_with_idle_status_even_if_source_was_running`, `duplicate_starts_with_null_active_worktree_even_if_source_had_one`, `duplicate_default_name_appends_copy_suffix`, `duplicate_with_custom_name_uses_input`, `duplicate_with_empty_custom_name_returns_invalid_input`, `duplicate_with_missing_source_returns_not_found`, `duplicate_does_not_touch_memory_tables`

これにより spec 13.4 API checklist と 7.4 Agent 複製 Acceptance の Rust 側要件は単体テストレベルで満たされている。

## Phase 2A Deliverables Summary

ファイル実在とシンボル参照を Grep で確認済み。

| File                              | Phase 2A Contribution                                                                                                                                                                                                                                                  |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src-tauri/src/domain/agent.rs`   | `AGENT_STATUS_VALUES` 定数 (4 値 `idle`/`running`/`error`/`needs_input`)、`validate_agent_status` 関数、`AgentDuplicateInput` 構造体 (`#[serde(rename_all = "camelCase")]`) と単体テスト 7 件                                                                          |
| `src-tauri/src/db/repo/agent.rs`  | `create` / `update` 内で status ホワイトリスト検証、`duplicate` 関数 (元 Agent の role/adapter_type/prompt_path/config_path を継承、id/created_at/updated_at を新規採番、status=idle 強制、active_worktree_id=null 強制、memory テーブル非 touch)、単体テスト 約 12 件 |
| `src-tauri/src/commands/agent.rs` | `agent_duplicate` Tauri command (`AgentDuplicateInput` を受け取り `Agent` DTO を返す)                                                                                                                                                                                  |
| `src-tauri/src/lib.rs`            | `invoke_handler` に `agent_duplicate` を登録                                                                                                                                                                                                                           |

spec §6 の command シグネチャと §5.2 の enum ホワイトリスト方針に整合。

## Known Issues

なし。

- Build / Test / Clippy 全て clean
- 回帰 0 (Phase 1 の既存 39 件もそのまま pass に含まれている)
- 軽微 clippy fix すら不要だった (3 行以下修正許可枠は未使用)

## Next Step (Phase 2B 引き渡し)

Phase 2A バックエンドは Phase 2B (フロント bindings.ts / agentService / agentStore / AgentCreateForm / AgentEditForm / LeftSidebar Agents セクション統合 / 複製確認 UI) に引き渡せる状態。

Phase 2B 着手時の確認事項:

1. `src-tauri/src/commands/agent.rs` の `agent_duplicate` シグネチャと spec §6 の TS 型定義 `AgentDuplicateInput { sourceAgentId: string; name?: string | null }` を bindings.ts に追加
2. `AgentStatus` 型 (`'idle' | 'running' | 'error' | 'needs_input'`) を bindings.ts に追加し、`Agent.status` を厳格化 (または `AgentStatus | string` ユニオン運用) するかを Phase 2B 内で決定
3. `agentService.duplicate(input)` と `agentStore.duplicateAgent(input)` (+ refresh) を追加
4. `AgentCreateForm` / `AgentEditForm` の status を自由文字列 → select に変更 (物理的に範囲外入力不可)
5. 複製確認 UI で「memory 非引継ぎ / status=idle / active worktree=未割当」を明示 (spec §7.4, §13.1)
6. LeftSidebar Agents セクションを実 store 経由の一覧に差し替え (spec §4.7, §13.2)
7. Phase 2D の Smoke チケットで実 Tauri 起動検証を予定（本チケットでは実施しない）
