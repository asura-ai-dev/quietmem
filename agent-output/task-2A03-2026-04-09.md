# task-2A03: AgentDuplicateInput DTO 追加 (Rust) - handoff

## Spec Alignment

本チケットは spec.md §6「Tauri commands 追加」および §2.4「型定義 (bindings.ts / Rust DTO)」で定義された `AgentDuplicateInput` の Rust 側 DTO に対応する。Phase 2 の後続チケット (task-2A04 repo 関数 / task-2A05 Tauri command) が参照する前提の型を先行定義する位置付け。

## サマリ

- `src-tauri/src/domain/agent.rs` に `pub struct AgentDuplicateInput` を追加
  - `source_agent_id: String`
  - `name: Option<String>` (`#[serde(default)]`)
  - `#[derive(Debug, Clone, Deserialize)]`
  - `#[serde(rename_all = "camelCase")]`
- 既存 `#[cfg(test)] mod tests` に単体テスト 2 件を追加
  - `agent_duplicate_input_deserializes_from_camel_case`
  - `agent_duplicate_input_name_is_optional`
- `Agent` / `AgentCreateInput` / `AgentUpdateInput` には一切変更を加えていない
- `Cargo.toml` は変更なし (`serde_json = "1"` は既に `[dependencies]` にあるため dev-dependencies 追記は不要)

## 変更ファイル

- `/Users/kzt/Desktop/project-d/product/quietmem/src-tauri/src/domain/agent.rs`
  - L125 付近 (AgentUpdateInput の直後) に `AgentDuplicateInput` を追加
  - `#[cfg(test)] mod tests` 末尾に 2 件のテスト追加

## コマンド結果

### cargo build

```
   Compiling quietmem v0.1.0 (/Users/kzt/Desktop/project-d/product/quietmem/src-tauri)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 1.48s
```

pass。

### cargo test --lib

```
test result: ok. 50 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.08s
```

pass。新規追加の 2 件も含む:

- `domain::agent::tests::agent_duplicate_input_deserializes_from_camel_case ... ok`
- `domain::agent::tests::agent_duplicate_input_name_is_optional ... ok`

task-2A01 / task-2A02 で追加された既存 48 件 (`validate_agent_status_*`, `create_with_invalid_status_returns_invalid_input`, `update_with_invalid_status_returns_invalid_input` 等) も全て pass を維持。

### cargo clippy --all-targets -- -D warnings

```
   Compiling quietmem v0.1.0 (/Users/kzt/Desktop/project-d/product/quietmem/src-tauri)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 1.55s
```

pass (警告なし)。

## 進捗出力

### completed

- `AgentDuplicateInput` 構造体の定義 (source_agent_id + optional name, camelCase deserialize)
- テスト `agent_duplicate_input_deserializes_from_camel_case`
- テスト `agent_duplicate_input_name_is_optional`
- `cargo build` / `cargo test --lib` / `cargo clippy --all-targets -- -D warnings` の全 pass 確認

### in_progress

- なし

### blocked

- なし

### tests_run

- `cargo build`: pass
- `cargo test --lib`: 50 passed / 0 failed (新規 2 件を含む)
- `cargo clippy --all-targets -- -D warnings`: pass

### known_gaps

- フロント側 `AgentDuplicateInput` 型 (bindings.ts) は task-2B01 で追加予定 (本チケット scope 外)
- `repo::agent::duplicate` 実装 + Tauri command は task-2A04 / task-2A05 で追加予定 (本チケット scope 外)

### next_step

1. task-2A04: `repo::agent::duplicate` 関数を実装し `AgentDuplicateInput` を受けて新 Agent を作成する (status=idle / active_worktree_id=null / memory 非引継ぎ)
2. task-2A05: `agent_duplicate` Tauri command を追加し invoke_handler に登録
