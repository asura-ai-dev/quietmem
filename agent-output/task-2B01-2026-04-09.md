# task-2B01 handoff (2026-04-09)

## Spec Alignment

spec.md §2.4 (型定義 / bindings.ts) と §5.2 (status enum 化) のフロント側起点。`AgentStatus` ホワイトリスト型と `AgentDuplicateInput` を bindings.ts に追加し、有効値・ラベル定数を `src/features/agents/agentStatus.ts` に集約することで、Phase 2 受け入れ条件 §7.5 (status 4 値表示) と §7.4 (Agent 複製) の TS 側土台を提供する。

## サマリ

- `src/types/bindings.ts` に `AgentStatus` 型ユニオンと `AgentDuplicateInput` インターフェースを追加し、`Agent.status` を `AgentStatus | string` (前方互換)、`AgentCreateInput.status` / `AgentUpdateInput.status` を `AgentStatus` (optional) に厳格化した。
- 新規ファイル `src/features/agents/agentStatus.ts` を作成し、`AGENT_STATUS_VALUES` (4 値, `as const satisfies readonly AgentStatus[]`)、`AGENT_STATUS_LABELS` (`needs_input` → "needs input")、`isAgentStatus` 型 guard を集約した。
- 既存 `AgentCreateForm.tsx` と `AgentEditForm.tsx` が status バリデーション厳格化で型エラーを起こしていたため、最小 1 行の `as AgentStatus` interim cast を入れて build を通した。両方ともコメントで「task-2C03 で本格修正 (select 化)」を明示している。
- `pnpm tsc --noEmit` (root reference + tsconfig.app.json 双方) と `pnpm build` がいずれも exit 0 で通過することを確認。

## ファイル一覧

### 編集

- `/Users/kzt/Desktop/project-d/product/quietmem/src/types/bindings.ts`
  - `AgentStatus` 型ユニオン追加
  - `Agent.status` を `AgentStatus | string` ユニオンに変更
  - `AgentCreateInput.status` / `AgentUpdateInput.status` を `AgentStatus` (optional) に厳格化
  - `AgentDuplicateInput` インターフェース追加
- `/Users/kzt/Desktop/project-d/product/quietmem/src/features/agents/AgentCreateForm.tsx`
  - `AgentStatus` 型 import 追加
  - `createAgent` 呼び出しの `status` フィールドに `as AgentStatus` interim cast (1 行追加 + comment、既存ロジック不変)
- `/Users/kzt/Desktop/project-d/product/quietmem/src/features/agents/AgentEditForm.tsx`
  - `AgentStatus` 型 import 追加
  - `updateAgent` 呼び出しの `status` フィールドに `as AgentStatus` interim cast (1 行 + comment、既存ロジック不変)

### 新規

- `/Users/kzt/Desktop/project-d/product/quietmem/src/features/agents/agentStatus.ts`
  - `AGENT_STATUS_VALUES` (`as const satisfies readonly AgentStatus[]`)
  - `AGENT_STATUS_LABELS` Record (idle/running/error/needs input)
  - `isAgentStatus` type guard

## 検証

### `pnpm tsc --noEmit` (root reference build)

```
EXIT=0
```

### `pnpm tsc --noEmit -p tsconfig.app.json` (アプリ単体)

interim cast 適用前:

```
src/features/agents/AgentCreateForm.tsx(118,9): error TS2322: Type 'string' is not assignable to type 'AgentStatus | undefined'.
src/features/agents/AgentEditForm.tsx(134,9): error TS2322: Type 'string' is not assignable to type 'AgentStatus | undefined'.
EXIT=2
```

interim cast 適用後:

```
EXIT=0
```

### `pnpm build` (任意確認)

```
> quietmem@0.1.0 build
> tsc -b && vite build

vite v5.4.21 building for production...
✓ 84 modules transformed.
dist/index.html                   0.39 kB │ gzip:  0.27 kB
dist/assets/index-BDJ6IO62.css   37.83 kB │ gzip:  5.49 kB
dist/assets/index-CfSmisMa.js   182.62 kB │ gzip: 57.24 kB
✓ built in 306ms
```

EXIT 0 で完了。

## 既知の留意点 (known_gaps)

- `AgentCreateForm.tsx` / `AgentEditForm.tsx` の status は依然「自由文字列 input」のままで、interim cast で型エラーを抑えているだけ。ユーザーが UI から `idle/running/error/needs_input` 以外を入力すると Rust 側 `InvalidInput` でフォーム送信が失敗する状態は Phase 2C (task-2C03) で select 化することで解消される予定。
- bindings.ts の `Agent.status` は前方互換のため `AgentStatus | string` ユニオン。今後 DB データが完全クリーンになれば `AgentStatus` 単独に厳格化できるが、Phase 2 ではユニオンを維持する設計判断を継続。
- `agentStore.ts` は status 文字列に直接触っていないため、本チケットの厳格化による副作用なし。

## Next step

- task-2B02: `agentService.duplicate()` の追加 (`agent_duplicate` Tauri command 呼び出し ラッパー)
- task-2B03: `agentStore.duplicateAgent()` action 追加
- task-2C03: `AgentCreateForm` / `AgentEditForm` の status を `<select>` 化し、本チケットの interim cast (`as AgentStatus`) を撤去
