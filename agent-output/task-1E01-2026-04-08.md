# task-1E01 handoff: WorkspaceRoute と 5 領域 Grid レイアウト

## Spec Alignment

spec.md §4.4 Workspace Shell / §5.2 受け入れ条件 (5 領域 Header / LeftSidebar / MainTabs / RightPanel / BottomDrawer の常時表示) の骨格部分。本チケットはレイアウト枠のみで、各領域の中身は後続チケット (1E02 以降) で実装する。

## Files Changed

- 新規: `/Users/kzt/Desktop/project-d/product/quietmem/src/routes/WorkspaceRoute.tsx`
- 新規: `/Users/kzt/Desktop/project-d/product/quietmem/src/routes/WorkspaceRoute.module.css`
- 新規: `/Users/kzt/Desktop/project-d/product/quietmem/src/types/css-modules.d.ts` (CSS Modules の型宣言。tsc がモジュール解決できるよう必要)
- 編集: `/Users/kzt/Desktop/project-d/product/quietmem/src/App.tsx` (`<WorkspaceRoute />` を直接レンダリングするよう変更)

## 実装詳細

- `WorkspaceRoute.module.css` の `.root` クラスで CSS Grid を定義:
  - `grid-template-columns: 240px 1fr 320px;`
  - `grid-template-rows: 48px 1fr 32px;` (drawer 行は ui-shell.md の指示通り初期 32px)
  - `grid-template-areas: "header header header" / "left main right" / "drawer drawer drawer"`
- 5 スロットに `grid-area` を割り当て、各スロットに `--bg-surface` 背景と `--border-subtle` のボーダーを適用 (隣接スロット間で重ならないよう header は bottom、left は right、main は left+right、right は left、drawer は top のみ)
- semantic HTML タグを採用: `<header>` / `<aside>` (left) / `<main>` / `<aside>` (right) / `<footer>` (drawer)
- `App.tsx` から `<WorkspaceRoute />` を直接レンダリング (ルーティング分岐は task-1E05 で追加予定)

## completed

- `src/routes/WorkspaceRoute.tsx` 作成 (CSS Grid + 5 placeholder スロット)
- `src/routes/WorkspaceRoute.module.css` 作成 (ui-shell.md の grid-template に準拠)
- `src/types/css-modules.d.ts` 作成 (`*.module.css` の型宣言)
- `src/App.tsx` を `<WorkspaceRoute />` を返す形に変更
- `pnpm tsc --noEmit` クリア
- `pnpm build` クリア (`tsc -b && vite build` 完走)

## in_progress

- なし

## blocked

- なし

## tests_run

- `pnpm tsc --noEmit`: pass (エラー出力なし、exit 0)
- `pnpm build`: pass (vite build 成功、`dist/assets/index-*.css` 3.28 kB / `index-*.js` 143.02 kB を生成)
- 視覚確認: 未実施 (Phase 1 のフロントエンドは手動確認可。本チケット時点では別作業者が起動して見るで十分)

## known_gaps

- CSS Modules 用の `*.module.css` 型宣言ファイルがプロジェクトに無かったため、`src/types/css-modules.d.ts` を新設した。これは task-1D05 のスコープに含まれていなかったが、CSS Modules を採用する以上不可欠なので本チケットで一緒に追加した。後続の他コンポーネントでも同じ宣言を流用できる
- drawer のトグル機構は未実装 (ui-shell.md に記載があるが本チケットの scope 外。後続チケットで対応)
- `Header` / `LeftSidebar` 等の専用コンポーネントは未作成 (チケット scope 通り、placeholder のみ)

## next_step

- task-1E02 以降で各スロットの中身 (Header, LeftSidebar, MainTabs, RightPanel, BottomDrawer) を実装
- BottomDrawer の開閉状態 (`drawerOpen`) を `uiStore` で管理し、`grid-template-rows` の最終行を 32px ↔ 240px で切り替えるトランジションを追加
- task-1E05 で `uiStore.route` ベースのルート分岐を `App.tsx` に導入し、`<WorkspaceRoute />` への直接レンダリングを置き換え
