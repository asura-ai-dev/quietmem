# task-2B05 handoff: uiStore に selectedAgentId 昇格

## Spec Alignment

spec.md §4.7 LeftSidebar 統合 / §10 未確定事項「`selectedAgentId` をどこに置くか」の対応として `uiStore` に `selectedAgentId` を昇格。Phase 2C で LeftSidebar Agents セクションと OverviewTab 編集フォームが同一の真実源を共有する基盤を準備する。

## Summary

`src/store/uiStore.ts` の `UiState` インターフェースおよび `useUiStore` 実装に `selectedAgentId: string | null` 状態と `setSelectedAgentId: (id: string | null) => void` action を追加した。既存 state (route / activeTab / drawerOpen / drawerTab) には一切触れていない。OverviewTab.tsx / LeftSidebar.tsx の差し替えは Phase 2C (task-2C05 / 2C06) で行うため、本チケットでは store 側のみの実装に留めた。

### 実装内容

- `UiState` interface
  - 追加: `selectedAgentId: string | null;`
  - 追加: `setSelectedAgentId: (id: string | null) => void;`
- `useUiStore` 実装
  - 初期値: `selectedAgentId: null`
  - action: `setSelectedAgentId: (selectedAgentId) => set({ selectedAgentId })`
- JSDoc コメントに `selectedAgentId` の意図 (Phase 2C で LeftSidebar / OverviewTab から共有される真実源) を追記

### 設計判断の踏襲

- 個別 selector hook (`useSelectedAgentId` 等) は導入しない (zustand 標準パターン)
- Project 切り替え時のリセット連動は本チケット範囲外 (task-2C06 で対応)

## ファイル一覧

- 編集: `/Users/kzt/Desktop/project-d/product/quietmem/src/store/uiStore.ts`

## 動作確認

### `pnpm tsc --noEmit`

実行結果: exit 0 (型エラーなし)

```
$ pnpm tsc --noEmit
(出力なし)
```

### 既存 OverviewTab への影響

- 本チケットの時点では OverviewTab.tsx はローカル state を継続使用しており、uiStore の `selectedAgentId` を参照していない (チケット明示通り)
- store 側にプロパティと action を追加しただけのため既存コードへの破壊的変更なし

## known_gaps

- task-2C06 で OverviewTab.tsx のローカル state を `useUiStore` 経由に差し替える必要あり
- task-2C05 で LeftSidebar Agents セクションが `useUiStore.selectedAgentId` を参照する必要あり
- Project 切り替え時の `selectedAgentId` リセット連動は task-2C06 で OverviewTab の useEffect から `setSelectedAgentId(null)` を呼ぶ想定

## Next step

- Orchestrator が `/verify tasks/phase-2B/task-2B05.md` を実行して done_when を確認する
- Phase 2C (task-2C05 / 2C06) で LeftSidebar / OverviewTab の差し替えを行う

---

## 進捗出力 (6 項目)

### completed

- `src/store/uiStore.ts` の `UiState` interface に `selectedAgentId` / `setSelectedAgentId` を追加
- `useUiStore` 実装に初期値 `null` と action を追加
- JSDoc コメント更新
- `pnpm tsc --noEmit` exit 0 確認

### in_progress

- なし

### blocked

- なし

### tests_run

- `pnpm tsc --noEmit`: pass (exit 0、型エラーなし)
- `pnpm build`: 未実行 (本チケットは store の最小拡張のみで型チェックで十分と判断。必要なら Orchestrator が `/verify` 時に追加実行)

### known_gaps

- OverviewTab.tsx / LeftSidebar.tsx の uiStore 連携は Phase 2C で実施 (本チケットの out of scope)

### next_step

1. Orchestrator が `/verify tasks/phase-2B/task-2B05.md` を実行
2. Phase 2C で task-2C05 (LeftSidebar 統合) / task-2C06 (OverviewTab 差し替え) を実装
