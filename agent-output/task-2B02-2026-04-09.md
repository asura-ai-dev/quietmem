# task-2B02 handoff (2026-04-09)

## Spec Alignment

spec.md §6 (Tauri commands 追加) と §12 実装順序 step 6 (`agentService.duplicate(...)` 追加) に対応。
QTM-003 Phase 2 における Agent 複製機能のフロント service 層を提供する。

## Summary

`src/services/agentService.ts` に `duplicate(input: AgentDuplicateInput): Promise<Agent>` を追加。
Tauri command `agent_duplicate` を `{ input }` でラップする invoke ラッパーで、Phase 1 既存の create / update のシグネチャパターン（第 2 引数 `{ input }`）と一貫性を保つ。

`AgentDuplicateInput` 型を `../types/bindings` から import に追加（アルファベット順で `AgentCreateInput` の次、`AgentUpdateInput` の前に挿入）。

## ファイル一覧

- 編集: `/Users/kzt/Desktop/project-d/product/quietmem/src/services/agentService.ts`
  - import 文に `AgentDuplicateInput` 追加
  - `duplicate` メソッド追加
- 参照のみ:
  - `/Users/kzt/Desktop/project-d/product/quietmem/src/types/bindings.ts` (task-2B01 で `AgentDuplicateInput` 定義済み)
  - `/Users/kzt/Desktop/project-d/product/quietmem/agent-docs/agent-duplicate-design.md` (§TS service / store)
  - `/Users/kzt/Desktop/project-d/product/quietmem/agent-docs/spec.md` (§6 / §12)
  - `/Users/kzt/Desktop/project-d/product/quietmem/tasks/phase-2B/task-2B02.md`

## 実装内容詳細

```ts
import { invoke } from "@tauri-apps/api/core";
import type {
  Agent,
  AgentCreateInput,
  AgentDuplicateInput,
  AgentUpdateInput,
} from "../types/bindings";

const agentService = {
  create: (input: AgentCreateInput): Promise<Agent> =>
    invoke<Agent>("agent_create", { input }),
  listByProject: (projectId: string): Promise<Agent[]> =>
    invoke<Agent[]>("agent_list_by_project", { projectId }),
  update: (input: AgentUpdateInput): Promise<Agent> =>
    invoke<Agent>("agent_update", { input }),
  duplicate: (input: AgentDuplicateInput): Promise<Agent> =>
    invoke<Agent>("agent_duplicate", { input }),
} as const;

export { agentService };
```

- 第 2 引数: `{ input }` で統一（spec / Phase 1 既存パターンと整合）
- 戻り値: `Promise<Agent>`（新規作成された Agent DTO）
- service 層は invoke ラッパーに徹し、エラー変換 / フォールバックは行わない（store 層 = task-2B03 の責務）
- `as const` 維持
- 既存 `create` / `listByProject` / `update` のシグネチャ変更なし

## 検証結果

### `pnpm tsc --noEmit`

```
exit=0
```

### `pnpm build`

```
> quietmem@0.1.0 build /Users/kzt/Desktop/project-d/product/quietmem
> tsc -b && vite build

vite v5.4.21 building for production...
transforming...
✓ 84 modules transformed.
rendering chunks...
computing gzip size...
dist/index.html                   0.39 kB │ gzip:  0.27 kB
dist/assets/index-BDJ6IO62.css   37.83 kB │ gzip:  5.49 kB
dist/assets/index-CHOcX6zC.js   182.67 kB │ gzip: 57.26 kB
✓ built in 305ms
exit=0
```

両方とも exit 0 で pass。

## completed

- `src/services/agentService.ts` への `AgentDuplicateInput` import 追加
- `agentService.duplicate` メソッド実装
- `pnpm tsc --noEmit` exit 0 確認
- `pnpm build` exit 0 確認

## in_progress

なし

## blocked

なし

## tests_run

- `pnpm tsc --noEmit` → exit 0
- `pnpm build` → exit 0（vite build 成功、84 modules transformed）

## known_gaps

- service 層のエラーハンドリングは意図的に未実装（store 層 task-2B03 で対応）
- 実 Tauri command 呼び出しの動作確認は Phase 2D の UI 統合または手動 Smoke で行う

## next_step

1. task-2B03: `agentStore.duplicateAgent(...)` の追加（refresh 含む store 層実装）
2. その後 Phase 2C / 2D で UI 側に複製ボタンと確認 UI を統合
