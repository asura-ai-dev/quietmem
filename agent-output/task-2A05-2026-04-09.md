# task-2A05 handoff (2026-04-09)

## Spec Alignment

spec.md §2.2 / §6 / §13.4 の `agent_duplicate` Tauri command を追加する要件に対応。
フロント側から `invoke('agent_duplicate', { input })` で呼び出せるよう、薄い command と invoke_handler 登録を提供する。

## Summary

- `src-tauri/src/commands/agent.rs` に `agent_duplicate` 関数を追加 (薄いアダプタ。バリデーションは repo 層に委譲)
- use に `AgentDuplicateInput` を import 追加
- `src-tauri/src/lib.rs` の `tauri::generate_handler!` に `commands::agent::agent_duplicate` を `agent_update` の直後に登録
- 既存 invoke_handler の他のエントリには変更なし
- Phase 1 既存パターンどおり `state.with_conn(|conn| db::repo::agent::duplicate(conn, input))` で dispatch
- task-2A04 で repo 層 `duplicate` 関数は実装済み・テスト済みのため、command 層では薄い配線のみ

## Files Changed

- `/Users/kzt/Desktop/project-d/product/quietmem/src-tauri/src/commands/agent.rs`
  - L19: use に `AgentDuplicateInput` を追加
  - L58-71: `agent_duplicate` 関数を追加 (agent_update の直後)
- `/Users/kzt/Desktop/project-d/product/quietmem/src-tauri/src/lib.rs`
  - L34: invoke_handler に `commands::agent::agent_duplicate` を追加

## Commands Run

### `cargo build`

```
Compiling quietmem v0.1.0 (/Users/kzt/Desktop/project-d/product/quietmem/src-tauri)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 1.42s
```

Pass.

### `cargo test --lib`

```
test result: ok. 58 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.09s
```

Pass. task-2A04 の `duplicate_returns_new_agent_with_copied_fields` 含む全 58 件が成功。

### `cargo clippy --all-targets -- -D warnings`

```
Compiling quietmem v0.1.0 (/Users/kzt/Desktop/project-d/product/quietmem/src-tauri)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 1.30s
```

Pass. warning / error なし。

## 実装の詳細

### commands/agent.rs の diff 概要

```rust
use crate::domain::agent::{Agent, AgentCreateInput, AgentDuplicateInput, AgentUpdateInput};
```

```rust
#[tauri::command(rename_all = "camelCase")]
pub fn agent_duplicate(state: State<'_, AppState>, input: AgentDuplicateInput) -> AppResult<Agent> {
    state.with_conn(|conn| db::repo::agent::duplicate(conn, input))
}
```

rustfmt により関数シグネチャが 1 行に整形されているが機能差はない。
doc コメントで「元 Agent の role/adapter_type/prompt_path/config_path を引き継ぎ、status は idle、active_worktree_id は null」「memory テーブルには一切書き込まない」を明示。

### lib.rs の diff 概要

```rust
commands::agent::agent_update,
commands::agent::agent_duplicate,   // 追加
commands::worktree::worktree_create,
```

`agent_update` の直後、`worktree_create` の前に 1 行追加。

## 動作確認

- `cargo build` および `tauri::generate_handler!` macro expansion が compile 成功することで、
  `agent_duplicate` が正しく Tauri invoke_handler に登録されたことを確認
- repo 層の duplicate 関数は task-2A04 で `:memory:` DB テストが pass 済み
  (`duplicate_returns_new_agent_with_copied_fields` が今回の実行でも pass)
- command 層は薄いアダプタのため単体テストは追加しない (out of scope)

## Known gaps

なし。チケットの scope (commands/agent.rs + lib.rs) はすべて満たしている。

## Next step

- task-2B02: bindings.ts に `AgentStatus` / `AgentDuplicateInput` 型を追加 (フロント bindings の整備)
- task-2B03: `agentService.duplicate(...)` および `agentStore.duplicateAgent(...)` の追加
- いずれもフロント側の作業で、バックエンド側からは本チケットで invoke 可能な状態が整っている

## Verification note

- `*.done_when.md` は generator から隔離されているため本セッションでは一切参照していない
- `/verify` による done_when 検証は Orchestrator の責務
