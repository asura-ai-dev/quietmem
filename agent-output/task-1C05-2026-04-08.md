# task-1C05 Handoff — Tauri Builder へ commands を登録

## Spec Alignment

spec.md §2.2 / §4.3 / §5.1 / §11.2 — Phase 1C で定義した Project / Agent / Worktree の 9 つの Tauri commands を `tauri::Builder::invoke_handler` に登録し、フロントエンドから invoke 可能にする。これにより受け入れ条件「各 command がフロントエンドから呼び出し可能」の前提を成立させる。

## Phase

Phase 1C / task-1C05 (Tauri Builder へ commands を登録)

## completed

- `src-tauri/src/lib.rs` の `run()` 関数に `invoke_handler(tauri::generate_handler![...])` を追加
- 以下 9 つの commands を登録:
  - `commands::project::project_create`
  - `commands::project::project_list`
  - `commands::project::project_update`
  - `commands::agent::agent_create`
  - `commands::agent::agent_list_by_project`
  - `commands::agent::agent_update`
  - `commands::worktree::worktree_create`
  - `commands::worktree::worktree_list_by_project`
  - `commands::worktree::worktree_update`
- 既存の `AppState::initialize()` / `eprintln!` / `manage(state)` / `setup` ロジックは保持
- warning ゼロを維持

## in_progress

なし

## blocked

なし

## tests_run

- `cargo check` (src-tauri) — pass, 0 warnings
- `cargo test` (src-tauri) — pass (39 passed, 0 failed, 0 warnings)
- `cargo build` (src-tauri) — pass, 0 warnings
- 手動検証 (`pnpm tauri dev` からの `window.__TAURI__.core.invoke("project_list")`) — 未実行 (Rust 側で generate_handler! マクロ展開が通っており、ビルドが成功しているため done_when 上は十分)

## known_gaps

- 実際に `pnpm tauri dev` を起動した手動検証は行っていない。チケットの Implementation Notes では「ビルドが通ることを確認できれば十分」とあるため、本タスクのスコープでは完了扱い。統合確認は Phase 1C 後段の evaluator / smoke test で行う想定。
- 並列実行中の task-1D01 (bindings.ts) とは Rust / TypeScript でファイルが完全に分離されており、衝突なし。

## Files Changed

- `/Users/kzt/Desktop/project-d/product/quietmem/src-tauri/src/lib.rs` — `invoke_handler` に 9 commands を登録

## next_step

- Orchestrator による `/verify tasks/phase-1C/task-1C05.md` の実行
- task-1D01 (bindings.ts) の完了を待って Phase 1D 以降で TypeScript 側 services 層と統合
- Phase 1 smoke test フェーズで実際に invoke が成功することを統合確認
