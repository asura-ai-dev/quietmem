# task-2B06: Phase 2B 検証 (pnpm tsc / build)

## Spec Alignment

spec.md §2.4 (型定義 / bindings.ts) と §6 (Tauri commands 追加) を対象とした Phase 2B 全タスクの完了確認チケット。
フロントエンド型 / service / store / トークンが Phase 2C (UI 強化) に引き渡せる状態か、`pnpm tsc --noEmit` と `pnpm build` で機械検証する。

## 現在フェーズ

Phase 2B (フロントエンド基盤拡張) 全タスク完了後の最終ゲート検証 — task-2B06 (pnpm tsc / build pass 確認)

## 実行コマンドと結果

| コマンド            | 結果   | 備考                                                         |
| ------------------- | ------ | ------------------------------------------------------------ |
| `pnpm tsc --noEmit` | exit 0 | 型エラーなし。task-2B01 で入れた interim cast はそのまま温存 |
| `pnpm build`        | exit 0 | `tsc -b && vite build` の 2 段階とも成功                     |

### `pnpm build` 詳細

```
> quietmem@0.1.0 build /Users/kzt/Desktop/project-d/product/quietmem
> tsc -b && vite build

vite v5.4.21 building for production...
transforming...
✓ 84 modules transformed.
rendering chunks...
computing gzip size...
dist/index.html                   0.39 kB │ gzip:  0.27 kB
dist/assets/index-DkZKSWnW.css   38.02 kB │ gzip:  5.54 kB
dist/assets/index-D-CQ1NTS.js   182.91 kB │ gzip: 57.29 kB
✓ built in 290ms
```

- **modules**: 84 (Phase 1 完了時点と一致 — Phase 2B はファイル追加なし or 新規 1 ファイル `agentStatus.ts` のみで、`tokens.css` は既に main.tsx 経由で取り込まれているため module 数は据え置き)
- **CSS**: 38.02 kB (red パレット 3 色 + danger alias の追加分は微増のみ)
- **JS**: 182.91 kB
- **build time**: 290 ms

## Phase 2B 全成果物のサマリ

### 1. `src/types/bindings.ts` — `AgentStatus` / `AgentDuplicateInput` 追加

- L46: `export type AgentStatus = "idle" | "running" | "error" | "needs_input";`
- L57: `Agent.status: AgentStatus | string;` (前方互換ユニオン採用 — DB に既存範囲外値が残るケース対策)
- L71: `AgentCreateInput.status?: AgentStatus;` (厳格化済み)
- L81: `AgentUpdateInput.status?: AgentStatus;`
- L93–96: `AgentDuplicateInput { sourceAgentId: string; name?: string | null; }`

### 2. `src/features/agents/agentStatus.ts` (新規)

- L13–18: `AGENT_STATUS_VALUES` (`as const satisfies readonly AgentStatus[]`)
- L24–29: `AGENT_STATUS_LABELS` (`needs_input → "needs input"`)
- L35–36: `isAgentStatus` type guard
- import は `bindings.ts → agentStatus.ts` の片方向のみ。循環なし

### 3. `src/services/agentService.ts` — `duplicate` メソッド

- L23–24: `duplicate: (input: AgentDuplicateInput): Promise<Agent> => invoke<Agent>("agent_duplicate", { input })`
- 既存 4 メソッド (create / listByProject / update + duplicate) と同一の `{ input }` 渡しパターン

### 4. `src/store/agentStore.ts` — `duplicateAgent` action

- L51: AgentState インターフェースに `duplicateAgent: (input: AgentDuplicateInput) => Promise<Agent>;`
- L103–114: 実装本体。`agentService.duplicate(input)` 呼出 → 戻り値 `agent.projectId` で対応 project の `refreshAgents` を実行 → 楽観更新なし、エラー時は store error state に詰めて throw。create / update と同一パターン

### 5. `src/store/uiStore.ts` — `selectedAgentId` + `setSelectedAgentId`

- L24: `selectedAgentId: string | null;` (state)
- L32: `setSelectedAgentId: (id: string | null) => void;` (action)
- L53: 初期値 `null`
- L60: 実装 `setSelectedAgentId: (selectedAgentId) => set({ selectedAgentId })`
- コメント (L43–46) で「Phase 2C で LeftSidebar Agents セクションと OverviewTab の編集フォームから同一の真実源として参照される」と明示

### 6. `src/styles/tokens.css` — red パレット 3 色 + danger alias

- L23–26: red パレット
  - `--color-red-300: #f08a8a;`
  - `--color-red-500: #d04848;`
  - `--color-red-700: #8e2828;`
- L37–39: semantic alias
  - `--color-danger: var(--color-red-500);`
  - `--color-danger-strong: var(--color-red-700);`
  - `--color-danger-bg: var(--color-red-700);`

## tests_run

- `pnpm tsc --noEmit` → exit 0 (型エラー 0 件)
- `pnpm build` → exit 0 (84 modules transformed, vite v5.4.21, 290 ms)

## known_gaps / 既知の問題

なし。

- 型エラーなし
- module 数は Phase 1 完了時点と完全一致 (84)
- 不合格条件 (4 行以上の型修正 / render-time エラー / build 失敗) のいずれにも該当しない
- コード変更ゼロ (検証のみで完了)
- `bindings.ts ↔ agentStatus.ts` の循環 import なし (片方向のみ確認)

## next_step

- **Phase 2C (UI 強化) へ引き渡し**: Phase 2B で整備した型 / store / tokens を消費して以下を実装する
  1. `AgentCreateForm` の status を `AGENT_STATUS_VALUES` 由来の `<select>` に変更 (task-2B01 の interim cast 解消もここで)
  2. `AgentEditForm` の status select 化 + 「複製」ボタン + 確認 UI (memory 非引継ぎ明示)
  3. `LeftSidebar` Agents セクションを `useUiStore.selectedAgentId` 経由で実 Agent 一覧に差し替え (Phase 1 placeholder 撤去)
  4. `AgentList` (Overview) に status バッジ (4 値) + 編集 / 複製ボタン
  5. status バッジは `--color-danger` / `--accent-primary` / `--accent-attention` / `--fg-muted` を参照 (raw 色値直書き禁止)
- **Phase 2D Smoke** へは Phase 2C 完了後に進む (実 Tauri 起動 + Smoke Flow 全通し + 再起動後保持確認)
