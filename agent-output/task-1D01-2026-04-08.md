# task-1D01 handoff — TypeScript 型ミラー bindings.ts

## Spec Alignment

spec.md §4.3 Tauri commands (CRUD) / §11.2 Evaluator Checklist (API) で要求される「各 command の入出力に型がついている」を成立させるための前提ファイル。Rust 側 DTO (task-1C04) と 1 対 1 対応する TS 型を提供し、後続の services 層と UI 層が型安全に invoke を呼べる土台にする。

## Files Changed

- `src/types/bindings.ts` (new)
  - `AppErrorPayload` (code union + message)
  - `Project` / `ProjectCreateInput` / `ProjectUpdateInput`
  - `Agent` / `AgentCreateInput` / `AgentUpdateInput`
  - `Worktree` / `WorktreeCreateInput` / `WorktreeUpdateInput`
  - 合計 10 型を export。すべて `export interface`
  - 純粋型定義のみ（ロジック・関数なし）
  - フィールド名は camelCase（Rust `#[serde(rename_all = "camelCase")]` と一致）
  - `agent-docs/tauri-commands.md` L62-L195 の TypeScript 型定義セクションを 1 文字違わず転記

## 検証

- `pnpm tsc --noEmit` → exit 0（エラーなし）
- ファイルは `export interface` のみで構成され、ランタイムコードを含まない

## Null / Optional の扱い（確認済み）

- `Agent.activeWorktreeId`: `string | null`
- `Agent.promptPath` / `Agent.configPath`: `string | null`
- `Worktree.agentId`: `string | null`
- Create 系 の optional フィールドは `?: T | null` または `?: T`
- Update 系の変更可能フィールドは `?:` で表現（未指定=変更なし）

## completed

- src/types/ ディレクトリ作成
- bindings.ts 新規作成（10 型 export）
- tauri-commands.md L62-L195 と完全一致の転記
- pnpm tsc --noEmit pass

## in_progress

- なし

## blocked

- なし

## tests_run

- `pnpm tsc --noEmit` → exit 0（プロジェクト全体で型エラーなし）

## known_gaps

- なし。このチケットは純粋な型定義ファイルで、scope は完結している
- services 層 (projectService / agentService / worktreeService) からの import は後続チケット (task-1D02 以降) の責務

## next_step

- 後続 (task-1D02 等) の services 層実装で `../types/bindings` から import して invoke<T>() の型パラメータに流用する
