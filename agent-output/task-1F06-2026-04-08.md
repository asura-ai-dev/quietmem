# task-1F06 handoff (2026-04-08)

## Spec Alignment

zustand selector が毎レンダー新しい `[]` を返して `useSyncExternalStore`
の identity 比較を破壊し、`Maximum update depth exceeded` で Workspace
全体がブランクだった回帰を修正する。これにより spec.md §5.1
(Project / Agent / Worktree CRUD UI が動作する) と §5.2 (Workspace
Shell の 5 領域が描画される) と §9 (Smoke Flow が GUI 上で完遂できる)
の達成条件が回復する。

## Phase

- current: Phase 1F implementation (task-1F06)
- ticket: tasks/phase-1F/task-1F06.md

## completed

- `src/features/agents/AgentList.tsx`
  - モジュールトップに `EMPTY_AGENTS: readonly Agent[]` /
    `EMPTY_WORKTREES: readonly Worktree[]` を追加
  - 2 つの zustand selector を `?? EMPTY_AGENTS` / `?? EMPTY_WORKTREES`
    に変更
  - `Agent` / `Worktree` 型を `../../types/bindings` から import
- `src/features/worktrees/WorktreeList.tsx`
  - `EMPTY_WORKTREES: readonly Worktree[]` を追加
  - selector を `?? EMPTY_WORKTREES` に変更
  - `Worktree` 型を import
- `src/tabs/OverviewTab.tsx`
  - `EMPTY_AGENTS` / `EMPTY_WORKTREES` を追加
  - 2 つの selector を `selectedProjectId` 分岐の両側で同じ stable 定数
    を返すよう変更 (selectedProjectId が null でも snapshot identity が
    安定)
  - 型 import を追加
- `src/features/agents/AgentEditForm.tsx`
  - `worktrees` prop を `Worktree[]` → `readonly Worktree[]` に変更
    (OverviewTab から渡る `readonly Worktree[]` を受け取れるように)。
    内部では mutation していないので read-only 化のみで型差は解消
- `src/components/ErrorBoundary.tsx` 新規作成
  - React class component
  - props: `children`, 任意の `fallback`
  - `getDerivedStateFromError` / `componentDidCatch` 実装
  - デフォルト fallback はテーマトークンを使った静かな案内カード
- `src/components/ErrorBoundary.module.css` 新規作成
  - `.root`: 100vh / center / `var(--bg-app)`
  - `.card`: max-width 480px / `var(--space-6)` padding /
    `var(--border-subtle)` border / `var(--radius-lg)`
  - `.eyebrow`: uppercase / letter-spacing / `var(--color-amber-500)` /
    `var(--font-size-sm)`
  - `.title`: `var(--font-size-xl)` / `var(--fg-primary)`
  - `.message`: `var(--color-amber-500)`
  - `.hint`: `var(--fg-muted)`
- `src/App.tsx`
  - `ErrorBoundary` を import
  - `WorkspaceRoute` のみ `<ErrorBoundary>` でラップ (FirstRun /
    Dashboard / Settings は ticket 指示通りスキップ)

## in_progress

- なし

## blocked

- なし

## tests_run

- `pnpm tsc --noEmit` → exit 0 (clean)
- `pnpm build` → success (vite build, 84 modules transformed,
  dist/assets/index-CfSmisMa.js 182.62 kB)
- ランタイム検証 (`?review=1` で Workspace が描画される確認) は
  Orchestrator が agent-browser で行う (generator スコープ外)

## known_gaps

- ランタイム検証 (`?review=1` で実際に Workspace が描画されること、
  AgentList / WorktreeList が空状態を正しく表示すること、
  `Maximum update depth exceeded` 警告が出ないこと) は Orchestrator が
  実機で確認する必要がある
- ErrorBoundary は Workspace 1 経路のみに入れた。FirstRun /
  Dashboard / Settings は ticket 指示通りスキップ。後続フェーズで他経路
  にも入れたい場合は別チケット化

## next_step

1. Orchestrator が `/verify tasks/phase-1F/task-1F06.md` を走らせる
2. Orchestrator が `?review=1` で agent-browser スクショを撮影し、
   Workspace が描画されることを確認する
3. evaluator を再実行し、Phase 1F 全体の合格を確認する

## Files Changed

- /Users/kzt/Desktop/project-d/product/quietmem/src/features/agents/AgentList.tsx
- /Users/kzt/Desktop/project-d/product/quietmem/src/features/worktrees/WorktreeList.tsx
- /Users/kzt/Desktop/project-d/product/quietmem/src/tabs/OverviewTab.tsx
- /Users/kzt/Desktop/project-d/product/quietmem/src/features/agents/AgentEditForm.tsx
- /Users/kzt/Desktop/project-d/product/quietmem/src/App.tsx
- /Users/kzt/Desktop/project-d/product/quietmem/src/components/ErrorBoundary.tsx (new)
- /Users/kzt/Desktop/project-d/product/quietmem/src/components/ErrorBoundary.module.css (new)
