# task-2A02 Handoff (2026-04-09)

## Spec Alignment

spec.md §2.2「status enum 化 (バックエンド側の緩い検証)」および §5.2「アプリケーション層の status enum」の write 経路組み込みに該当。`agent_create` / `agent_update` から 4 値以外の status を `InvalidInput` で reject する要件 (§7.5) を repo 層で満たす。

## 実装サマリ

`src-tauri/src/db/repo/agent.rs` の write 経路 2 箇所に task-2A01 で追加済みの `validate_agent_status` を組み込み、4 値以外 (`idle` / `running` / `error` / `needs_input` 以外) の status を `AppError::InvalidInput` で reject するようにした。

変更点:

1. `use` 文を `use crate::domain::agent::{Agent, AgentCreateInput, AgentUpdateInput, validate_agent_status};` に拡張 (rustfmt によって `validate_agent_status, Agent, AgentCreateInput, AgentUpdateInput` の順に自動ソートされた)
2. `create` 関数の status デフォルト適用直後に `validate_agent_status(&status)?;` を追加
3. `update` 関数の `new_status` 決定直後に `validate_agent_status(&new_status)?;` を追加
4. `#[cfg(test)] mod tests` に 4 件の新規テストを追加

スキーマ / DTO / 既存の型シグネチャは変更していない。既存 `update_with_none_fields_preserves_existing_values`（`running` を投入） や `create_then_list_by_project_returns_one_agent`（status=None → `idle`）は範囲内のため影響なし。

## 触ったファイル一覧

- `/Users/kzt/Desktop/project-d/product/quietmem/src-tauri/src/db/repo/agent.rs`
  - use 文拡張 (`validate_agent_status` 追加)
  - `create` に `validate_agent_status(&status)?;` を追加
  - `update` に `validate_agent_status(&new_status)?;` を追加
  - tests モジュールに 4 件追加:
    - `create_with_invalid_status_returns_invalid_input`
    - `create_accepts_all_four_status_values`
    - `update_with_invalid_status_returns_invalid_input`
    - `update_accepts_needs_input_status`

## 実行したコマンドと結果

### cargo build

```
cd src-tauri && cargo build
   Compiling quietmem v0.1.0
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 1.38s
```

warning / error なし。

### cargo test --lib

```
cd src-tauri && cargo test --lib
running 48 tests
...
test db::repo::agent::tests::create_with_invalid_status_returns_invalid_input ... ok
test db::repo::agent::tests::create_accepts_all_four_status_values ... ok
test db::repo::agent::tests::update_with_invalid_status_returns_invalid_input ... ok
test db::repo::agent::tests::update_accepts_needs_input_status ... ok
...
test db::repo::agent::tests::update_with_none_fields_preserves_existing_values ... ok
...
test result: ok. 48 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.08s
```

関連テスト抜粋:

- 新規 4 件 すべて ok
- 既存 `update_with_none_fields_preserves_existing_values` (status=`running` を投入) も ok — 既存動作を維持
- `create_then_list_by_project_returns_one_agent`、`create_applies_default_values` (status=None → `idle` デフォルト) も ok
- `validate_agent_status_*` (domain 側の task-2A01 テスト) 5 件も引き続き ok

総数 48 pass / 0 fail（Phase 1F 完了時の 44 件 + 今回追加 4 件）。

### cargo clippy --all-targets -- -D warnings

```
cd src-tauri && cargo clippy --all-targets -- -D warnings
   Compiling quietmem v0.1.0
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 1.11s
```

warning / error なし (`-D warnings` で昇格するものはゼロ)。

## 既知の問題

なし。チケット scope の範囲で完結している。以下は仕様通りの想定挙動であり問題ではない:

- `update` は既存 DB 行の status が範囲外 (Phase 1 以前の未検証データ) の場合、ユーザーが他フィールドだけ更新しようとしても `existing.status` のマージで reject される可能性がある。これは `agent-docs/phase-2-status-enum.md` §write 経路への組み込み / spec.md §5.2 「シンプルに常にバリデート」方針通り。Phase 1 のテストデータ (`running`) は範囲内のため実害なし。

## Next step

- task-2A03: `agent_create` / `agent_update` Tauri command 側のエラー伝播テスト（必要なら）
- task-2A04: `repo::agent::duplicate` 実装（本チケットで追加した `validate_agent_status` を duplicate 経路でも利用する想定）
- task-2A05: `agent_duplicate` Tauri command 追加
