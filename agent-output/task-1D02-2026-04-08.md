# Handoff: task-1D02 services 層 (invoke ラッパー)

## Spec Alignment

spec.md §2.2 バックエンド層 / §4.3 Tauri commands (CRUD) / §11.2 API 観点に対応。
Phase 1 の Project / Agent / Worktree CRUD を UI 層から型付きで呼び出せるよう、
`@tauri-apps/api/core` の invoke を camelCase API にラップした services 層を提供する。

## Current Phase

- Phase: 1D (frontend services 層)
- Ticket: task-1D02
- Depends on: task-1D01 (bindings 型), task-1C05 (invoke_handler 登録)
- Iteration: 0

## Completed

- `src/services/` ディレクトリを新規作成
- `src/services/projectService.ts` 実装
  - `create(input: ProjectCreateInput) -> Promise<Project>` (`invoke("project_create", { input })`)
  - `list() -> Promise<Project[]>` (`invoke("project_list")`)
  - `update(input: ProjectUpdateInput) -> Promise<Project>` (`invoke("project_update", { input })`)
- `src/services/agentService.ts` 実装
  - `create(input: AgentCreateInput) -> Promise<Agent>` (`invoke("agent_create", { input })`)
  - `listByProject(projectId: string) -> Promise<Agent[]>` (`invoke("agent_list_by_project", { projectId })` フラット渡し)
  - `update(input: AgentUpdateInput) -> Promise<Agent>` (`invoke("agent_update", { input })`)
- `src/services/worktreeService.ts` 実装
  - `create(input: WorktreeCreateInput) -> Promise<Worktree>` (`invoke("worktree_create", { input })`)
  - `listByProject(projectId: string) -> Promise<Worktree[]>` (`invoke("worktree_list_by_project", { projectId })` フラット渡し)
  - `update(input: WorktreeUpdateInput) -> Promise<Worktree>` (`invoke("worktree_update", { input })`)
- いずれも `const xxxService = { ... } as const; export { xxxService };` 形式で export
- 型は `../types/bindings` から import、invoke は `@tauri-apps/api/core` から import
- `pnpm tsc --noEmit` 型チェック成功 (exit 0, エラー 0)

## Files Changed

- `src/services/projectService.ts` (新規)
- `src/services/agentService.ts` (新規)
- `src/services/worktreeService.ts` (新規)
- `agent-output/task-1D02-2026-04-08.md` (本ファイル)

## In Progress

なし

## Blocked

なし

## Tests Run

- `pnpm tsc --noEmit` -> pass (exit code 0, 型エラーなし)
  - bindings.ts の既存 10 型との整合性を TypeScript コンパイラが確認
  - invoke のジェネリクス型引数も型チェック済み
- 実機 invoke の動作確認は未実施（Tauri runtime が必要なため、UI 層統合時に実施予定）

## Known Gaps

- services 層は invoke の reject を捕捉せず、そのまま上位に throw する素通しラッパー
  - tauri-commands.md の指示通り `AppErrorPayload` 形でそのまま投げる方針
  - 上位 UI 層 (zustand store / components) でエラーハンドリングする前提
- 実機 invoke 呼び出しの smoke test は未実施。Tauri runtime 起動時に UI 層から試される

## Next Step

1. phase 1D の次チケット (store / UI 層) へ進行
2. UI 層統合時に実機 invoke smoke test を実施

## Notes / 重要判断

- tauri-commands.md L213-L233 の例を厳密に踏襲
  - `create` / `update` 系は `{ input }` ラップ
  - `list_by_project` 系はフラット `{ projectId }` 渡し
  - `list()` は引数なし (第 2 引数省略)
- `as const` + 名前付き export パターンで tree-shaking と型推論の両立
- エラー変換は行わない（services は純粋な薄いラッパー）
